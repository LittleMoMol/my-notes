# 1 基本设定
## 1-1 区域
- 目标区域是**半径 1800 米**的圆形区域。
- 所有干扰源位于目标区域**内** 
- 目标区域圆心是坐标**原点** 
- x 轴正方向为**东** 
- y 轴正方向为**北** 
- 坐标单位为米
- 机器狗可以向模拟器**提交目标区域外的位置**：模拟器仍会按提交的位置计算移动耗时和检测、清除结果
- 每个坐标分量必须是有限数值，且绝对值不得超过 2000000 米。NaN、正负无穷大和超出范围的坐标返回 HTTP 400，请求不执行，也不推进位置或虚拟时间。
## 1-2 方位角
- 正东方向 $0^\circ$ 
- 逆时针方向为正。
- 正南方向 $270^\circ$ 
- 角度范围 $[0, 360)$ 
- `/measure` 返回的 `svd_deg` 表示干扰源的**示向度**，单位为度
## 1-3 频道
- 有效频道范围 $\{1, 2, \cdots, 20\}$ 
- **每个频道最多对应一个干扰源**，很重要！
- 一次测试中干扰源总数量在 10 到 16 个之间。
- 具体有多少个干扰源不会通过接口直接返回，机器狗程序需要根据检测和清除结果自行判断。
- `/measure` 的 `channel` 表示测向机要检测的频道。
- `/clear` 的 `channel` 表示要尝试清除的目标干扰源频道。
	- *注意*：`/clear` 的 `channel` 不会改变测向机当前频道，也不会产生切换频道耗时。
## 1-4 初始状态
- 初始位置 $(0,0)$ 
- 测向机初始频道：1
- 机器狗必须逐次发送动作，并在收到本次完整 HTTP 响应后再发送下一个新动作。正常串行合法请求没有请求速率上限；每次检测的 5 秒只增加虚拟时间，不要求在现实中等待 5 秒。
## 1-5 其他
- 模拟器默认服务地址：`http://127.0.0.1:2026` 
- 选手可以在模拟器空闲时修改端口
# 2 关键物理规则
## 2-1 最大可被有效接收距离
每个干扰源都有一个最大可被有效接收距离，简称**有效接收半径**。
- 该距离在 1000 米到 1500 米之间。
- 距离超过该干扰源的有效接收半径时，机器狗在该位置接收不到该频道信号。
- 该距离不是固定常数，不同干扰源可能不同。
- 模拟器接口不会返回某个干扰源的有效接收半径。
## 2-2 全向与定向干扰源
干扰源分为全向和定向两类。
- **全向干扰源**：检测点到干扰源的距离不超过有效接收半径时，可以检测到信号。
- **定向干扰源**：检测点必须同时满足距离条件，并且位于该干扰源的有效覆盖角度范围内，才能检测到信号。有效覆盖角度范围为 $180^\circ$ (包含边界)，定向方向未知。

因此，`/measure` 返回无信号时，只能说明当前位置接收不到该频道信号。可能原因包括：
- 该频道没有未清除干扰源。
- 距离超过有效接收半径。
- 该频道干扰源是定向干扰源，但当前位置不在它的覆盖范围内。
## 2-3 示向度误差
`/measure` 返回的 `svd_deg` 是示向度，不是干扰源的精确真实方位。
- `svd_deg` 表示在检测点处测得的干扰源的示向度，单位为度。
- 模拟器返回的 `svd_deg` 与干扰源的最大场强方向的真实方位角之间存在一定的误差，参见题目正文。
- 误差范围为 $[-1^\circ, 1^\circ]$ 
- 返回值保留两位小数。
- 返回值仍按 $[0, 360)$ 归一化。
- 机器狗程序不应把单次 `svd_deg` 当作完全精确的真实方位。
## 2-4 近距离阈值与清除半径
- 近距离阈值为 5 米。检测距离不超过 5 米并且位于干扰源信号覆盖角度范围内时，由于信号太强，将无法获得示向度信息，`/measure` 返回 `measure_result="near"`，不返回 `svd_deg`。
- 清除半径为 20 米。清除位置与目标干扰源距离小于等于 20 米时，`/clear` 可以清除成功。注：光学精确定位及清除操作是否成功仅与清除点与干扰源之间的距离有关，与定向干扰源的朝向无关。
# 3 指令
![[Pasted image 20260910220630.png|650]]
移动和切换频道没有单独的接口：
- 移动由 `/measure` 或 `/clear` 请求中的 `position` 体现。
- 切换测向机频道只由 `/measure` 请求中的 `channel` 体现。
# 4 虚拟时间计算方法
只有请求被模拟器接受，即响应中 `accepted=true` 时，才可能推进虚拟时钟。
`accepted=false` 的响应里 `virtual_time_s` 为 0。不要把这个 0 当作当前虚拟时刻。需要记录虚拟时刻时，应使用最近一次 `accepted=true` 的响应。
`virtual_time_s` 是 `JSON number`，可能带小数，不应按整数解析。模拟器内部按微秒累计，响应最多保留6位小数，并删除无意义的末尾零。
# 5 通信规则
## 5-1 请求格式
4 条指令都使用 `HTTP+JSON`。
- 方法：`POST` 
- 请求头：`Content-Type` 必须是 `application/json`，可以带且只允许参数 `charset=utf-8`。出现其他参数时返回 `HTTP 415`。
- `Content-Encoding` 可以省略，或为 `identity`。其他值返回 `HTTP 415`。
- 请求体必须是无 BOM 的 UTF-8 JSON 对象，不能有重复键；JSON 嵌套不超过 16 层；请求体最大 65536 字节。
- 路径必须精确为 `/enter`、`/measure`、`/clear` 或 `/exit`，不接受尾随斜线和查询参数。
- 请求体必须是 UTF-8 JSON 对象，不能有重复键；请求体最大 65536 字节。
- `channel` 必须是 1 至 20 的整数。写成 `JSON number 1.0` 且数值恰好为整数时也会被接受；如写成 `1.5` 则返回 `HTTP 400`。
- 未声明的 JSON 字段不会被忽略，而是返回 `HTTP 200` 和 `accepted=false`，用于帮助发现字段拼写错误。
- `arena_id` 必须是ASCII字符串“`default`”。
- `robot_id` 必须逐字节等于当前登录参赛队号，UTF-8 长度为 1 至 64 字节。
- `request_id` 是当前测试会话内的幂等键，UTF-8 长度为 1 至 128 字节。`robot_id` 和 `request_id` 不能包含控制字符或不可见格式字符。
## 5-2 响应格式
业务响应也是 JSON 对象。所有 JSON 业务响应都有以下字段：
![[Pasted image 20260911081200.png|600]]
如果 `accepted=false`，响应只包含上面 3 个字段，不包含检测结果、清除结果或退出原因。
## 5-3 HTTP 状态、业务接受状态
![[Pasted image 20260911081304.png|625]]
所有能够形成 HTTP 响应的错误都返回 JSON，其中至少包含 `accepted`、`real_timestamp_ms` 和 `virtual_time_s`。机器狗程序必须同时检查 HTTP 状态和 accepted，不能只判断其中一个。
每个新动作必须使用新的 `request_id`。只有在网络超时、连接中断等情况下重试完全相同的动作时，才复用原请求内容和原 `request_id`。
结构错误（`HTTP 400`）以及未知字段、`arena_id` / `robot_id` 不匹配（`HTTP 200` 且 `accepted=false`）不占用该 `request_id`，修正后可以复用。业务上已被接受的动作占用该 `request_id` 
# 6 `POST /enter` 
- **请求**：发送字段：
	![[Pasted image 20260911081949.png|600]]
	如
	```json
	{  
	  "arena_id": "default",  
	  "robot_id": "<参赛队号>",  
	  "request_id": "enter-1"  
	}
	```
- **响应**：`accepted=true` 时，返回字段：
	![[Pasted image 20260911082052.png|550]]
# 7 `POST /mearsure`
- **请求**：发送字段：
	![[Pasted image 20260911082142.png|625]]
	```json
	{  
	  "arena_id": "default",  
	  "robot_id": "<参赛队号>",  
	  "request_id": "measure-1",  
	  "position": {"x": 300, "y": 400},  
	  "channel": 1  
	}
	```
- **响应**：`accepted=true` 时，返回字段：
	![[Pasted image 20260911082253.png|525]]
- **三种检测结果**：
	- **无信号**：接收不到该频道的干扰源信号。可能原因包括**该频道没有未清除干扰源**、**距离超过最大可被有效接收距离**、**定向干扰源没有覆盖该位置**等。
		```json
		{  
		  "accepted": **true**,  
		  "real_timestamp_ms": 1760000000000,  
		  "virtual_time_s": 105,  
		  "measure_result": "no_signal"  
		}
		```
	- **距离过近**：已检测到该频道信号，但距离干扰源不超过 5 米。此时不返回 `svd_deg`。
		```json
		{  
		  "accepted": **true**,  
		  "real_timestamp_ms": 1760000000000,  
		  "virtual_time_s": 105,  
		  "measure_result": "near"  
		}
		```
	- **正常返回示向度**：已检测到该频道信号，并返回从检测点指向干扰源的方位角。`svd_deg` 保留两位小数，包含误差。
		```json
		{  
		  "accepted": **true**,  
		  "real_timestamp_ms": 1760000000000,  
		  "virtual_time_s": 105,  
		  "measure_result": "direction",  
		  "svd_deg": 123.45  
		}
		```
# `POST /clear` 
- **请求**：发送字段：
	![[Pasted image 20260911082552.png|575]]
	```json
	{  
	  "arena_id": "default",  
	  "robot_id": "<参赛队号>",  
	  "request_id": "clear-1",  
	  "position": {"x": 300, "y": 0},  
	  "channel": 3  
	}
	```
- **响应**：`accepted=true` 时，返回字段：
	![[Pasted image 20260911082642.png|550]]
- **两种清除结果** 
	- **成功清除**：
		```json
		{  
		  "accepted": true,
		  "real_timestamp_ms": 1760000000000,  
		  "virtual_time_s": 196,  
		  "clear_result": "success"  
		}
		```
	- **未发现可清除目标**：
		```json
		{  
		  "accepted": true,  
		  "real_timestamp_ms": 1760000000000,  
		  "virtual_time_s": 194,  
		  "clear_result": "no_target_in_range"  
		}
		```
# 9 `POST /exit` 
- **请求**：发送字段：
	![[Pasted image 20260911082839.png|575]]
	```json
	{  
	  "arena_id": "default",  
	  "robot_id": "<参赛队号>",  
	  "request_id": "exit-1"  
	}
	```
- **响应**：`accepted=true` 时，返回字段：
	![[Pasted image 20260911082919.png|600]]
	```json
	{  
	  "accepted": true,  
	  "real_timestamp_ms": 1760000000000,  
	  "virtual_time_s": 199,  
	  "exit_reason": "user_exit"  
	}
	```

