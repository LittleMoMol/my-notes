# 在 github 中建立新仓库
## 方法 1
1. 在 github 官网中新建仓库，并不要勾选 README.md，创建
2. 进入自己本地的项目目录
3. 本地创建 readme `echo "# 仓库名称" >> README.md` 
4. 初始化本地仓库 `git init` 
5. 添加文件 `git add .` 并提交 `git commit -m "Initial commit"` 
6. 关联远程仓库 `git remote add origin https://github.com/你的用户名/你的仓库名.git` 
7. 分支名称改为 main `git branch -M main` 
8. 推送代码到 github `git push -u origin main` (首次推送加 `-u` 会将本地分支与远程绑定，后续直接输 `git push` 即可)
	- 如果分支不是 `main` 而是 `master`，可以使用 `git branch -M main` 来改名字
# 更改 github 仓库的名称
1. 进入你的仓库主页，点击顶部的 Settings。在页面最上方的 Repository name​ 输入框里填入新名字，点击右侧的 Rename​ 确认即可。
2. 本地文件夹名不需要强制改，但建议改一下，尽量同名
3. 在本地仓库目录打开终端，先查看当前地址：`git remote -v` 
4. 去新仓库主页复制新的 HTTPS 或 SSH 地址，执行更新：`git remote set-url origin 新仓库地址` 
5. 再执行 `git remote -v` 确认显示的是新地址即可。
# 创建 python 虚拟环境
`python -m venv 虚拟环境名称` 
