# 规定
- 文件目录序号有两种。
	- `1, 2, ...` 型：这种为共识目录，里面放置的是我们可能需要、随用随查的文件，这些文件只可能让人类、AI 读取，不应该被任何程序读入，它们更改名字不应该影响整个项目的任何功能。可以中文命名。
	- `X1, X2, X3` 型：这种为队员工作与产出目录，这里面的文件夹名称与文件名不应该随意改动，因为可能会使得论文导入图片、代码导入数据等报错。其中 `X2_code` 和 `X3_essay` 中的所有文件命名必须为英文，`X1_modeling` 不做要求。
- *注意*：任何一方做完自己的任务后，且别人都在忙，那就去网站上查找相关文献，将论文或自己的学习成果产出，并放置在文件夹 `2_resource\参考文献` 或 `2_resource\其他资源` 中，可以中文命名，要见名知意。
# Day 0 准备工作
## 18:00：开始
接收题面并下载
## 19:00 前：选题
*需要一次讨论* 
读懂每道题目的题意，并做出选题
## 19:30 前：检查与就位
- **建模手** 
	1. 将所有问题和数据资料，整理成 AI 可读写 (`机`)、人类易看懂 (`人`) 的两种文件，并放置在文件夹 `1_question` 中，名称可以中文命名。
	- 编程手注意：这里的文件与数据不可被程序引用，如果需要，应该复制到自己工作目录下，并改为合适的英文命名。
- **编程手**：
	1. 根据建模手产出的文件，选出自己程序会用到的文件，复制到自己工作目录下，并改为英文命名。
	2. 调试自己的 python 环境，并检查 numpy、pandas、matplotlib 和其他相关库是否能够正确使用，如果不能，请及时修改。
- **论文手**：
	1. 调整 latex 模板 (如果需要的话)，创建论文，论文文件放置在文件夹 `X3_essay` 中，命名为 `essay_v1.tex`
	2. 确保自己可以插入图片、文献引用等。
## 睡前：图片与问题重述
- *讨论*：讨论出暂时需要画什么图，以及需要建立什么模型、用什么算法。并建立所有的**图片要求文档**，放置在 `X1_modeling\pic_info` 中。
- **建模手**：
	1. 填充图片要求文档，要求详见模板，放置在 `X1_modeling\pic_info` 中，命名为 `图片[编号]的要求文档` 供编程手编程。
	2. 给出第一版建模方案 (不含模型对比与优化，先做出来，要包含模型讲解，模型建立和求解算法是两码事)，产出到文件夹 `X1_modeling\model` 中，命名为 `建模v1：[内容].md`。
- **编程手**：
	1. 把能画的图画出来，产出代码与图片，命名全英文。代码放置在文件夹 `X2_code` 中，命名为 `pic_[编号]_[内容].py`；图片放置在文件夹 `X2_code\pic` 中，命名为 `pic_[编号]_[内容].png` 
- **论文手**：写完问题重述。
# Day 1 第一版论文产出至少 70%
- *本日目标*：将初步建模方案完整实现 70% 以上，注意：不需要模型对比，但需要最终答案。
- *要求*：论文要完整 (文献、附录、摘要除外)，结果要正确，做完所有问。有问题要随时更改建模。
- *注意*：有的时候是需要看网课、读论文等去学习相关知识的，不应忽略这一点。
- **建模手**：
	1. 进行第二次建模，产出到文件夹 `X1_modeling\model` 中，命名为 `建模v2：[与v1相比新增的内容].md`，这里要有模型的对比与优化。
	2. 产出第二次建模中相应的新的图片要求文档，及时通知大家。
	3. 需要帮助编程手、论文手做事，并且审核。
- **编程手**：
	1. 产出跑出结果和画图所需要的所有代码。代码名字全英文。除了一个放置常量的 py 文件之外可以被其他文件调用，建议每个文件实现独立运行功能。
- **论文手**：
	1. 根据建模手的模型、图片要求文档、编程手的代码与结果，写出最终论文，至少写完 70% 的内容。
	- 注意：引用的图片复制一份，放置在与自己论文同目录下
# Day 2 第一版论文产出、第二版论文产出
- **建模手**：
	1. 再想想还能优化什么，进行第三次建模，产出到文件夹 `X1_modeling\model` 中，命名为 `建模v3：[与v2相比新增的内容].md`。
	2. 产出第三次建模中相应的新的图片要求文档，及时通知大家。
	3. 需要帮助编程手、论文手做事，并且审核。
- **编程手**：
	1. 同 Day 1
- **论文手**：
	1. 完成第一版的论文产出，放置在文件夹 `X3_essay` 中，命名为 `essay_v1.tex`
	2. 完成第二版的论文产出，放置在文件夹 `X3_essay` 中，命名为 `essay_v2.tex`
# Day 3 第三版论文产出、语言优化
- **建模手**：
	1. 闲，帮忙，懂？
- **编程手**：
	1. 同 Day 1
- **论文手**：
	1. 完成第三版的论文产出，并经过*讨论*后产生摘要，加上附录，得到整体论文，放置在文件夹 `X3_essay` 中，命名为 `essay_v3.tex` 
- 最后的最后，一起检查论文并优化语言风格，产出最终版 `essay_v4.tex` 