# 1 随机事件与概率
## 1-1 事件域与概率的定义
- **事件域**（**$\sigma$ 域**、**$\sigma$ 代数**）：
- **概率的公理化定义**（**非负性公理**、**正则性公理**、**可列可加性公理**）、**概率空间**：


## 1-2 概率的可加性、单调性、加法公式
- **性质**：$P(\varnothing) =$ 
- **有限可加性**：
- **性质**：对任一事件 $A$，有 $P(\overline A) =$ 
- **性质**：若 $A\supset B$，则 $P(A-B) =$ 
	- **推论**（**单调性**）：
- **性质**：对任意两个事件 $A,B$，有 $P(A-B) =$ 
- **加法公式**：对任意两个事件 $A,B$，有
	- **推广**：对任意 $n$ 个事件 $A_1, A_2, \cdots, A_n$，有
	- **推论**（**半可加性**）：对任意两个事件 $A,B$，有
		- **推广**：对任意 $n$ 个事件 $A_1, A_2, \cdots, A_n$，有
## 1-3 概率的连续性
- 单调不减的**极限事件**：
- 单调不增的**极限事件**：
- **下连续**：
- **上连续**：
- **概率的连续性**：
- **性质**：若 $P$ 是 $\mathscr F$ 上满足 $P(\Omega) = 1$ 的非负集合函数，则它具有可列可加性 $\iff$ 
	- **推论**：在概率的公理化定义中，可以将可列可加性换成有限可加性和下连续性
## 1-4 条件概率、全概率公式、贝叶斯公式
- **条件概率**：
- **乘法公式**：
- **乘法公式（推广）**：
- **全概率公式**：
- **贝叶斯公式**：
## 1-5 独立性
- **相互独立**（**独立**）、**不独立**（**相依**）：对于事件 $A,B$，
	- 若事件 $A$ 与 $B$ 独立，则 $A$ 与 $\overline B$ 独立，$\overline A$ 与 $B$ 独立，$\overline A$ 与 $\overline B$ 独立
- **两两独立**、**相互独立**：设 $A,B,C$ 是三个事件，
- **相互独立**：设有 $n$ 个事件 $A_1, A_2, \cdots, A_n$，
	- **性质**：$n$ 个相互独立的事件中的任意一部分内仍是相互独立的；而且任意一部分与另一部分也是独立的
	- **性质**：将相互独立事件中的任一部分换为对立事件，所得的诸事件仍为相互独立的
- **相互独立**：设有两个试验 $E_1$ 和 $E_2$，
- **相互独立**：对于 $n$ 个试验 $E_1, E_2, \cdots, E_n$，
- **$n$ 重独立重复试验**：
- **$n$ 重伯努利(Bernoulli)试验**：
# 2 随机变量及其分布
## 2-1 随机变量、分布函数、分布列、密度函数
- **随机变量**：
- **离散随机变量**：
- **连续随机变量**：
- **分布函数**、**服从**：
- 任一分布函数 $F(x)$ 都具有下面三条性质：
	(1) **单调性**：
	(2) **有界性**：
	(3) **右连续性**：
- **概率分布列**（**分布列**）：设 $X$ 时一个离散随机变量，
- **分布列的基本性质**：
	- **非负性**：
	- **正则性**：
- **概率密度函数**（**密度函数**、**密度**）、**连续随机变量**、**连续分布函数**：设随机变量 $X$ 的分布函数为 $F(x)$，
- **密度函数的基本性质** 
	- **非负性**：
	- **正则性**：
- **性质**：连续随机变量的分布函数 $F(x)$ 一定是整个数轴上的连续函数
- **性质**：连续随机变量 $X$ 在 $(-\infty, \infty)$ 上任一点 $a$ 的概率恒为 $0$ 
## 2-2 数学期望、方差、标准差
- **数学期望**（**期望**、**均值**）：设离散随机变量 $X$ 的分布列为 $p(x_i) = P(X=x_i),\ i=1,2,\cdots,n,\cdots$，
- **数学期望**（**期望**、**均值**）：设连续随机变量 $X$ 的密度函数为 $p(x)$，
- **定理**：若随机变量 $X$ 的分布用分布列 $p(x_i)$ 或用密度函数 $p(x)$ 表示，则 $X$ 的某一函数 $g(X)$ 的数学期望为 $E[g(X)] =$ 
- **方差**：若随机变量 $X^2$ 的数学期望 $E(X^2)$ 存在，
- **标准差**：
- 方差的性质
	- **性质 1**：$\mathrm{Var}(X) =$ 
	- **性质 2**：常数的方差为 
	- **性质 3**：若 $a,b$ 是常数，则 $\mathrm{Var}(aX+b) =$ 
	- **性质 4**：若 $E(X^2)=0$，则
- **切比雪夫(Chebyshev)不等式**：设随机变量 $X$ 的数学期望和方差都存在，
- **大偏差**、**大偏差发生概率**：
- **定理**：若随机变量 $X$ 的方差存在，则 $\mathrm{Var}(X) = 0$ $\iff$ 
## 2-3 常用概率分布及其数学期望和方差总结
- 注：仅列出各分布密度函数的非零区域
- 0-1 分布
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 二项分布 $b(n,p)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 泊松分布 $P(\lambda)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 超几何分布 $h(n,N,M)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 几何分布 $Ge(p)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 负二项分布 $Nb(n,p)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 正态分布 $N(\mu, \sigma^2)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 均匀分布 $U(a,b)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 指数分布 $Exp(\lambda)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 伽马分布 $Ga(\alpha, \lambda)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 卡方分布 $\chi^2(n)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 贝塔分布 $Be(a,b)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 对数正态分布 $LN(\mu, \sigma^2)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 柯西分布 $Cau(\mu, \lambda)$ 
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
- 韦布尔分布
	- 分布列 $p_k$ 或分布密度 $p(x)$：
	- 期望：
	- 方差：
## 2-4 随机变量函数的分布
- 设 $X$ 是一个随机变量，那么 $Y=g(X)$ 作为 $X$ 的函数，同样也是一个随机变量。已知随机变量 $X$ 的分布，如何求出另一个随机变量 $Y=g(X)$ 的分布？
	$$
	\begin{aligned}
	\\
	\\
	\\
	\\
	\\
	\\
	\\
	\end{aligned}
	$$
- **正态变量的线性变换仍为正态变量**：设随机变量 $X$ 服从正态分布 $N(\mu, \sigma^2)$，则当 $a\not=0$ 时，有 $Y=aX+b \sim$ 
- **对数正态分布**：设随机变量 $X\sim N(\mu, \sigma^2)$，则 $Y=e^X$ 的概率密度函数为 $p_Y(y) =$ 
- **定理**：设随机变量 $X$ 服从伽马分布 $Ga(\alpha, \lambda)$，则当 $k>0$ 时，有 $Y=kX\sim$ 
## 2-8 分布的其他特征数
### 2-8-1 $k$ 阶矩
- **$k$ 阶原点矩**、**$k$ 阶中心矩**：
	- **中心矩和原点矩的关系**：
### 2-8-2 变异系数
- **变异系数**：
	- 量纲？
### 2-8-3 分位数
- **$p$ 分位数**、**下侧 $p$ 分位数**：
- **上侧 $p$ 分位数**：
- 分位数与上侧分位数是可以相互转换的，公式为
### 2-8-4 中位数
- **中位数**：
### 2-8-5 偏度系数
- **偏度系数**（**偏度**）、**正偏**（**右偏**）、**负偏**（**左偏**）：
	- 量纲？
- **偏态分布**、**右偏分布**、**左偏分布**（并画图）：
### 2-8-6 峰度系数
- **峰度系数**（**峰度**）：
	- 量纲？
- 任一正态分布 $N(\mu, \sigma^2)$ 的峰度 $\beta_k=$ 
	- $\beta_k>0$ 表示
	- $\beta_k<0$ 表示
- 若在上述定义中，分子分母各除以 $[\sigma(X)]^4$，并记 $X$ 的标准化变量为 $X^* = \dfrac{X-E(X)}{\sigma(X)}$，则 $\beta_k = \dfrac{E(X^*)^4}{[E(X^{*2})]^2} - 3 = E(X^{*4}) - E(U^4)$，其中 $E(X^{*2}) = \mathrm{Var}(X^*) = 1$，$U$ 为标准正态变量，$E(U^4) = 3$ 
- **几种常见分布的偏度与峰度** 
	- 均匀分布 $U(a,b)$ 的均值、方差、偏度、峰度：
	- 正态分布 $N(\mu,\sigma^2)$ 的均值、方差、偏度、峰度：
	- 指数分布 $Exp(\lambda)$ 的均值、方差、偏度、峰度：
	- 伽马分布 $Ga(\alpha,\lambda)$ 的均值、方差、偏度、峰度：