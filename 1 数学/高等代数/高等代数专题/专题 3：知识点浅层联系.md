# T3-1 矩阵与特征矩阵的联系
- 对于数字矩阵 $\boldsymbol A$，若 $|\lambda\boldsymbol E - \boldsymbol A|$ 在数域 $P$ 上能分解为一次因式的乘积，则：
	- $\boldsymbol A$ 的全体特征值的和为 $\text{tr } \boldsymbol A$ 
	- $\boldsymbol A$ 的全体特征值的积为 $|\boldsymbol A|$ 
- 相似矩阵有相同的特征多项式、有相同的最小多项式
- $\boldsymbol A$ 的特征多项式是 $\boldsymbol A$ 的一个零化多项式
- 
