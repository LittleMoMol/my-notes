# 1 初值问题 (IVP) 的数值解法
## 1-1 问题描述
用向后欧拉、改进欧拉、RK 方法等求解下列初值问题，分别取步长 $h=0.1, 0.001, 0.001$，计算 $y(t)$ 在 $t=1$ 处的近似值，并计算相应的截断误差和收敛精度
$$
\begin{aligned}
&(1).\ y' = e^{-t} - y,\ y(0) = 1\quad (y=e^{-t}(t+1)) \\
&(2).\ y' + y = y^2(\cos t - \sin t),\ y(0) = 1\quad (y=1 / (e^{t}-\sin t)) \\
\end{aligned}
$$
## 1-2 后退欧拉法
### 原理
从积分形式的初值问题出发：
$$y(t+h) = y(t) + \int_{t}^{t+h} f(\tau, y(\tau))\,d\tau$$
用**右矩形公式**近似积分：
$$\int_{t}^{t+h} f(\tau, y(\tau))\,d\tau \approx h\,f(t+h, y(t+h))$$
得到隐式格式：
$$y_{n+1} = y_n + h\,f(t_{n+1}, y_{n+1})$$
### 局部截断误差
$$R_n = \int_{t_n}^{t_{n+1}} f(t, y(t))\,dt - h\,f(t_{n+1}, y(t_{n+1})) = O(h^2)$$
该方法为**一阶方法**
### 数值实现
对于**线性问题** $y' = -y + g(t)$，可求出显式表达式：
$$y_{n+1} = \frac{y_n + h\,g(t_{n+1})}{1 + h}$$
例如问题 (1) 中 $g(t) = e^{-t}$，故：
$$
y_{n+1} = \frac{y_n + h\,e^{-(t_n+h)}}{1 + h}
$$
对于**非线性问题**，采用**不动点迭代**求解隐式方程：
$$
y_{n+1}^{(k+1)} = y_n + h\,f(t_{n+1}, y_{n+1}^{(k)}),\ k = 0, 1, 2, \dots
$$
当 $|h \cdot \partial f/\partial y| < 1$ 时迭代收敛。
### 算法步骤
```
输入：f(t,y), t0, y0, 目标时间 T, 步长 h
n = (T - t0) / h
t = t0, y = y0
for i = 1 to n:
    t_next = t + h
    y_next = y  // 初始猜测
    repeat  // 不动点迭代
        y_next = y + h * f(t_next, y_next)
    until 收敛
    y = y_next, t = t_next
返回 y
```

## 1-3 改进欧拉法
### 原理
同样从积分恒等式出发，用**梯形公式**近似积分：
$$
\int_{t}^{t+h} f(\tau, y(\tau))\,d\tau \approx \frac{h}{2}\bigl[f(t, y(t)) + f(t+h, y(t+h))\bigr]
$$
得到**预测-校正格式**：
- 预测步（显式欧拉）：$\tilde{y}_{n+1} = y_n + h\,f(t_n, y_n)$ 
- 校正步（梯形）：$y_{n+1} = y_n + \frac{h}{2}\bigl[f(t_n, y_n) + f(t_{n+1}, \tilde{y}_{n+1})\bigr]$ 
### 局部截断误差
$$
R_n = \int_{t_n}^{t_{n+1}} f(t, y(t))\,dt - \frac{h}{2}\bigl[f(t_n, y(t_n)) + f(t_{n+1}, y(t_{n+1}))\bigr] = O(h^3)
$$
该方法为**二阶方法** 
### 算法步骤
```
输入：f(t,y), t0, y0, 目标时间 T, 步长 h
n = (T - t0) / h
t = t0, y = y0
for i = 1 to n:
    k1 = f(t, y)
    k2 = f(t + h, y + h*k1)
    y = y + (h/2) * (k1 + k2)
    t = t + h
返回 y
```
## 1-4 经典四阶龙格-库塔法（RK4）
### 原理
在区间 $[t_n, t_{n+1}]$ 上取四个点的斜率加权平均，以得到更高精度的平均斜率近似：
$$
y_{n+1} = y_n + h\sum_{i=1}^{s} b_i k_i
$$
对于经典 RK4（$s=4$），权系数为 $b = [1/6, 2/6, 2/6, 1/6]$ 
### 格式
$$
\begin{aligned}
k_1 &= f(t_n, y_n) \\[2pt]
k_2 &= f\!\left(t_n + \frac{h}{2},\; y_n + \frac{h}{2}k_1\right) \\[2pt]
k_3 &= f\!\left(t_n + \frac{h}{2},\; y_n + \frac{h}{2}k_2\right) \\[2pt]
k_4 &= f\!\left(t_n + h,\; y_n + h\,k_3\right) \\[2pt]
y_{n+1} &= y_n + \frac{h}{6}\bigl(k_1 + 2k_2 + 2k_3 + k_4\bigr)
\end{aligned}
$$
### 局部截断误差
$$
R_n = O(h^5)
$$
该方法为**四阶方法**。
### 算法步骤
```
输入：f(t,y), t0, y0, 目标时间 T, 步长 h
n = (T - t0) / h
t = t0, y = y0
for i = 1 to n:
    k1 = f(t, y)
    k2 = f(t + h/2, y + (h/2)*k1)
    k3 = f(t + h/2, y + (h/2)*k2)
    k4 = f(t + h, y + h*k3)
    y = y + (h/6) * (k1 + 2*k2 + 2*k3 + k4)
    t = t + h
返回 y
```
## 1-5 数值实验结果
### (1) $y' = e^{-t} - y$，精确解 $y(1) = 0.7357588823$ 

| 步长 $h$ |    后退欧拉法     |    误差    |    改进欧拉法     |    误差    |     RK4      |    误差    |
| :----: | :----------: | :------: | :----------: | :------: | :----------: | :------: |
|  0.1   | 0.7271431369 | 8.62e-03 | 0.7357338534 | 2.50e-05 | 0.7357587469 | 1.35e-07 |
|  0.01  | 0.7348452814 | 9.14e-04 | 0.7357588592 | 2.32e-08 | 0.7357588823 | 1.17e-11 |
| 0.001  | 0.7356669738 | 9.19e-05 | 0.7357588823 | 2.30e-11 | 0.7357588823 | 1.89e-15 |

### (2) $y' + y = y^2(\cos t - \sin t)$，精确解 $y(1) = 0.5328187459$ 

| 步长 $h$ |    后退欧拉法     |    误差    |    改进欧拉法     |    误差    |     RK4      |    误差    |
| :----: | :----------: | :------: | :----------: | :------: | :----------: | :------: |
|  0.1   | 0.5145277541 | 1.83e-02 | 0.5339603326 | 1.14e-03 | 0.5328198002 | 1.05e-06 |
|  0.01  | 0.5308415451 | 1.98e-03 | 0.5328314886 | 1.27e-05 | 0.5328187460 | 1.12e-10 |
| 0.001  | 0.5326194711 | 1.99e-04 | 0.5328188746 | 1.29e-07 | 0.5328187459 | 1.05e-14 |

### 误差分析

1. **后退欧拉法**（一阶）：$h$ 缩小为 $1/10$，误差约缩小为 $1/10$，符合 $O(h)$ 收敛阶。
2. **改进欧拉法**（二阶）：$h$ 缩小为 $1/10$，误差约缩小为 $1/100$，符合 $O(h^2)$ 收敛阶。
3. **RK4 法**（四阶）：$h$ 缩小为 $1/10$，误差约缩小为 $1/10^4$，符合 $O(h^4)$ 收敛阶。

在相同步长下，RK4 精度最高，改进欧拉法次之，后退欧拉法精度最低。但后退欧拉法具有良好的数值稳定性（A-稳定），适用于刚性方程。

# 2 边值问题（BVP）的数值解法
## 2-1 问题描述
用差分法、有限元法、打靶法、配点法等求解下列边值问题。请写清楚具体的计算过程、格式对应的矩阵形式、并画出相应的数值解图。
### 问题一
取步长 $h = \pi/8, \pi/16$ 
$$
\begin{cases}
y'' + y = x, & 0 < x < \dfrac{\pi}{2} \\[4pt]
y(0) = y\!\left(\dfrac{\pi}{2}\right) = \dfrac{\pi}{2}
\end{cases}
$$
**精确解推导** 
齐次解 $y_h = A\cos x + B\sin x$，特解 $y_p = x$，故通解为 $y = A\cos x + B\sin x + x$ 
代入边界条件：
$$
y(0) = A = \frac{\pi}{2}, \qquad y\!\left(\frac{\pi}{2}\right) = B + \frac{\pi}{2} = \frac{\pi}{2} \Rightarrow B = 0
$$
因此：
$$
y(x) = x + \frac{\pi}{2}\cos x
$$
### 问题二
取步长 $h = 0.2, 0.1$ 
$$
\begin{cases}
y'' + 2y' - 3y = 3x + 1, & 1 < x < 2 \\[4pt]
y'(1) - y(1) = 1 \\[4pt]
y'(2) + y(2) = -4
\end{cases}
$$
**精确解推导：**
齐次方程特征方程 $r^2 + 2r - 3 = 0$，解得 $r = -3, 1$，故 $y_h = C_1 e^{-3x} + C_2 e^{x}$。
设特解 $y_p = Ax + B$，代入得：
$$
0 + 2A - 3(Ax + B) = 3x + 1 \Longrightarrow -3Ax + (2A - 3B) = 3x + 1
$$
解得 $A = -1$，$B = -1$，故 $y_p = -x - 1$。
通解 $y = C_1 e^{-3x} + C_2 e^{x} - x - 1$，代入边界条件得 $C_1 = C_2 = 0$。因此：
$$
y(x) = -x - 1
$$
该解是线性函数，所有数值方法均可精确（机器精度）重现。
## 2-2 差分法（Finite Difference Method）
### 方法原理
用差商近似导数，将微分方程离散为线性代数方程组。
**基本差分公式：**
- 二阶中心差分：$\displaystyle y''(x_i) \approx \frac{y_{i-1} - 2y_i + y_{i+1}}{h^2}$ 
- 一阶中心差分：$\displaystyle y'(x_i) \approx \frac{y_{i+1} - y_{i-1}}{2h}$ 
- 一阶前向差分：$\displaystyle y'(x_i) \approx \frac{y_{i+1} - y_i}{h}$ 
- 一阶后向差分：$\displaystyle y'(x_i) \approx \frac{y_i - y_{i-1}}{h}$ 

将差商代入微分方程，在 $x_i\;(i=1,2,\dots,N-1)$ 处离散，得到 $N-1$ 个代数方程，写成 $A\mathbf{y} = \mathbf{b}$ 形式求解。
### 应用于问题一
将二阶中心差分代入 $y'' + y = x$：
$$\frac{y_{i-1} - 2y_i + y_{i+1}}{h^2} + y_i = x_i$$
整理得：
$$y_{i-1} + (-2 + h^2)y_i + y_{i+1} = h^2 x_i$$
令 $N$ 为区间等分数，$h = \dfrac{\pi/2}{N}$，内点 $i = 1, 2, \dots, N-1$。
边界条件：$y_0 = \pi/2$，$y_N = \pi/2$。
矩阵形式 $A\mathbf{y} = \mathbf{b}$（$\mathbf{y} = [y_1, y_2, \dots, y_{N-1}]^T$）：
$$
\begin{bmatrix}
-2+h^2 & 1 & 0 & \cdots & 0 \\
1 & -2+h^2 & 1 & \cdots & 0 \\
0 & 1 & -2+h^2 & \cdots & 0 \\
\vdots & \vdots & \vdots & \ddots & \vdots \\
0 & 0 & \cdots & 1 & -2+h^2
\end{bmatrix}
\begin{bmatrix}
y_1 \\ y_2 \\ y_3 \\ \vdots \\ y_{N-1}
\end{bmatrix}
=
\begin{bmatrix}
h^2 x_1 - y_0 \\
h^2 x_2 \\
h^2 x_3 \\
\vdots \\
h^2 x_{N-1} - y_N
\end{bmatrix}
$$
### 应用于问题二
将二阶和一阶中心差分代入 $y'' + 2y' - 3y = 3x + 1$：
$$\frac{y_{i-1} - 2y_i + y_{i+1}}{h^2} + 2\cdot\frac{y_{i+1} - y_{i-1}}{2h} - 3y_i = 3x_i + 1$$
整理得：
$$\left(\frac{1}{h^2} - \frac{1}{h}\right)y_{i-1} + \left(-\frac{2}{h^2} - 3\right)y_i + \left(\frac{1}{h^2} + \frac{1}{h}\right)y_{i+1} = 3x_i + 1$$
**左边界** $y'(1) - y(1) = 1$，用前向差分：
$$\frac{y_1 - y_0}{h} - y_0 = 1 \quad \Longrightarrow \quad y_1 - (1+h)y_0 = h$$
**右边界** $y'(2) + y(2) = -4$，用后向差分：
$$\frac{y_N - y_{N-1}}{h} + y_N = -4 \quad \Longrightarrow \quad (1+h)y_N - y_{N-1} = -4h$$
令 $a = \dfrac{1}{h^2} - \dfrac{1}{h}$，$b = -\dfrac{2}{h^2} - 3$，$c = \dfrac{1}{h^2} + \dfrac{1}{h}$，得到完整矩阵 $A\mathbf{y} = \mathbf{b}$（$\mathbf{y} = [y_0, y_1, \dots, y_N]^T$）：
$$
\begin{bmatrix}
-(1+h) & 1 & 0 & \cdots & 0 & 0 \\
a & b & c & \cdots & 0 & 0 \\
0 & a & b & \cdots & 0 & 0 \\
\vdots & \vdots & \vdots & \ddots & \vdots & \vdots \\
0 & 0 & 0 & \cdots & b & c \\
0 & 0 & 0 & \cdots & -1 & 1+h
\end{bmatrix}
\begin{bmatrix}
y_0 \\ y_1 \\ y_2 \\ \vdots \\ y_{N-1} \\ y_N
\end{bmatrix}
=
\begin{bmatrix}
h \\
3x_1 + 1 \\
3x_2 + 1 \\
\vdots \\
3x_{N-1} + 1 \\
-4h
\end{bmatrix}
$$
### 数值结果
问题（一）$y'' + y = x$：

| $N$ |   $h$   |   最大误差   |      收敛阶      |
| :-: | :-----: | :------: | :-----------: |
|  4  | 0.39270（$\pi/8$） | 5.75e-03 |       —       |
|  8  | 0.19635（$\pi/16$） | 1.41e-03 | $O(h^{2.03})$ |
| 16  | 0.09817 | 3.54e-04 | $O(h^{1.99})$ |
| 32  | 0.04909 | 8.84e-05 | $O(h^{2.00})$ |
| 64  | 0.02454 | 2.21e-05 | $O(h^{2.00})$ |

差分法为二阶精度 $O(h^2)$，与理论一致。

**数值解图：**
![[fd_p1_solution.png|500]]
图 2-1：差分法求解问题（一）的数值解与精确解对比（$N=8,\ h=\pi/16$）。蓝色散点为数值解，红色实线为精确解 $y=x+\frac{\pi}{2}\cos x$。

---

问题（二）$y'' + 2y' - 3y = 3x + 1$：

| $N$ | $h$ | 最大误差 |
|:---:|:---:|:--------:|
| 4 | 0.25000 | 7.11e-15 |
| 5 | 0.20000 | 4.97e-15 |
| 8 | 0.12500 | 4.88e-15 |
| 10 | 0.10000 | 7.55e-15 |
| 16 | 0.06250 | 1.51e-14 |

由于精确解为线性函数，差分法可精确还原（误差为机器精度）。

**数值解图：**
![[fd_p2_solution.png|500]]
图 2-2：差分法求解问题（二）的数值解与精确解对比（$h=0.1$）。数值解与精确解 $y=-x-1$ 完全重合（机器精度）。
## 2-3 有限元法（Finite Element Method）
### 方法原理
有限元法的核心是将求解区域划分为有限个单元，在单元上用基函数的线性组合近似解，然后通过 Galerkin 加权余量法建立方程组。
对于二阶 ODE 边值问题 $y'' + p(x)y' + q(x)y = f(x)$：
**步骤一：网格剖分**
将区间 $[a, b]$ 剖分为 $N$ 个单元，$h = (b-a)/N$，节点 $x_i = a + ih$。
**步骤二：选取基函数**
采用分段线性基函数（帽子函数）$\phi_j(x)$：
$$\phi_j(x_i) = \delta_{ij} = \begin{cases} 1, & i = j \\ 0, & i \neq j \end{cases}$$
在每个单元 $[x_{i-1}, x_i]$ 上：
$$\phi_{i-1}(x) = \frac{x_i - x}{h}, \quad \phi_i(x) = \frac{x - x_{i-1}}{h}$$
近似解设为 $y_h(x) = \sum_{j=0}^{N} y_j \phi_j(x)$。
**步骤三：推导弱形式**
将微分方程乘以测试函数 $\phi_i$，在 $[a,b]$ 上积分，对二阶项分部积分。
对于 $y'' + y = x$，分部积分得：
$$\int_a^b y''\phi_i\,dx = \bigl[y'\phi_i\bigr]_a^b - \int_a^b y'\phi_i'\,dx$$
对于 Dirichlet 边界条件，测试函数在边界上为零，故边界项消失。弱形式为：
$$-\int_a^b y'\phi_i'\,dx + \int_a^b y\phi_i\,dx = \int_a^b x\phi_i\,dx$$
对于含 Robin 边界条件的问题，边界项 $[y'\phi_i]_a^b$ 不自动消失，需代入边界条件处理。
**步骤四：计算单元矩阵并组装**
在每个单元 $[x_{i-1}, x_i]$ 上计算局部贡献，组装成整体刚度矩阵 $A$ 和荷载向量 $\mathbf{f}$，求解 $A\mathbf{y} = \mathbf{f}$。
### 应用于问题一
**弱形式：**
$$-\int_0^{\pi/2} y'\phi_i'\,dx + \int_0^{\pi/2} y\phi_i\,dx = \int_0^{\pi/2} x\phi_i\,dx$$
**单元刚度矩阵（在单元 $[x_{i-1}, x_i]$ 上）：**
$$
K^{(e)} = -\frac{1}{h}
\begin{bmatrix}
1 & -1 \\
-1 & 1
\end{bmatrix}
+ \frac{h}{6}
\begin{bmatrix}
2 & 1 \\
1 & 2
\end{bmatrix}
=
\begin{bmatrix}
-\dfrac{1}{h} + \dfrac{h}{3} & \dfrac{1}{h} + \dfrac{h}{6} \\[6pt]
\dfrac{1}{h} + \dfrac{h}{6} & -\dfrac{1}{h} + \dfrac{h}{3}
\end{bmatrix}
$$
**单元荷载向量：**
$$\mathbf{f}^{(e)} = \frac{h}{6}\begin{bmatrix} 2x_{i-1} + x_i \\ x_{i-1} + 2x_i \end{bmatrix}$$
**整体刚度矩阵 $A$（$N-1$ 阶，对应内点 $y_1,\dots,y_{N-1}$）：**
对角线元素 $A_{ii} = \dfrac{2}{h} + \dfrac{2h}{3}$，次对角线 $A_{i,i+1} = A_{i+1,i} = \dfrac{1}{h} + \dfrac{h}{6}$（注意符号与原始文档不同）。
右端项需减去边界已知节点的贡献：
- 第 $1$ 行减去 $\left(\dfrac{1}{h} + \dfrac{h}{6}\right) y_0$
- 第 $N-1$ 行减去 $\left(\dfrac{1}{h} + \dfrac{h}{6}\right) y_N$
### 应用于问题二
**弱形式：**
对于 $y'' + 2y' - 3y = 3x + 1$，分部积分并考虑 Robin 边界条件 $y'(1)-y(1)=1$，$y'(2)+y(2)=-4$。
分部积分后得到边界项 $[y'\phi_i]_1^2 = y'(2)\phi_i(2) - y'(1)\phi_i(1)$。代入边界条件：
$$[y'\phi_i]_1^2 = (-y(2)-4)\phi_i(2) - (y(1)+1)\phi_i(1) = -y(2)\phi_i(2) - y(1)\phi_i(1) - 4\phi_i(2) - \phi_i(1)$$
最终弱形式：
$$-\int_1^2 y'\phi_i'\,dx + 2\int_1^2 y'\phi_i\,dx - 3\int_1^2 y\phi_i\,dx - y(1)\phi_i(1) - y(2)\phi_i(2) = \int_1^2 (3x+1)\phi_i\,dx + \phi_i(1) + 4\phi_i(2)$$
其中 $-y(1)\phi_i(1) - y(2)\phi_i(2)$ 项贡献到刚度矩阵的对角元（增加 $-1$），$\phi_i(1) + 4\phi_i(2)$ 贡献到右端项。
**单元刚度矩阵：**
对于 $-\int y'\phi_i' + 2\int y'\phi_i - 3\int y\phi_i$：
$$
K^{(e)} = -\frac{1}{h}
\begin{bmatrix}
1 & -1 \\ -1 & 1
\end{bmatrix}
+ 2\cdot\frac{1}{2}
\begin{bmatrix}
-1 & 1 \\ -1 & 1
\end{bmatrix}
- 3\cdot\frac{h}{6}
\begin{bmatrix}
2 & 1 \\ 1 & 2
\end{bmatrix}
=
\begin{bmatrix}
-\dfrac{1}{h} - 1 - h & \dfrac{1}{h} + 1 - \dfrac{h}{2} \\[6pt]
\dfrac{1}{h} - 1 - \dfrac{h}{2} & -\dfrac{1}{h} + 1 - h
\end{bmatrix}
$$
### 数值结果
问题（一）：

| $N$ | $h$ | 最大误差 |
|:---:|:---:|:--------:|
| 4 | 0.39270（$\pi/8$） | 5.47e-03 |
| 8 | 0.19635（$\pi/16$） | 1.39e-03 |
| 16 | 0.09817 | 3.53e-04 |
| 32 | 0.04909 | 8.84e-05 |
| 64 | 0.02454 | 2.21e-05 |
| 128 | 0.01227 | 5.53e-06 |

有限元法对问题（一）也是 $O(h^2)$ 收敛。

**数值解图：**
![[fem_p1_solution.png|500]]
图 2-3：有限元法求解问题（一）的数值解与精确解对比（$N=8,\ h=\pi/16$）。蓝色散点为数值解，红色实线为精确解。

---

问题（二）：

| $N$ | $h$ | 最大误差 |
|:---:|:---:|:--------:|
| 4 | 0.25000 | 0.00e+00 |
| 5 | 0.20000 | 0.00e+00 |
| 8 | 0.12500 | 4.00e-15 |
| 10 | 0.10000 | 2.22e-15 |
| 16 | 0.06250 | 1.78e-15 |

线性精确解由分段线性有限元精确重现。

**数值解图：**
![[fem_p2_solution.png|500]]
图 2-4：有限元法求解问题（二）的数值解与精确解对比（$h=0.1$）。数值解与精确解完全重合。
## 2-4 打靶法（Shooting Method）
### 方法原理
打靶法将边值问题转化为初值问题，通过迭代调整初始参数使另一端的边界条件满足。
对于 $\begin{cases} y'' = f(x, y, y'), & a < x < b \\ y(a) = \alpha, & y(b) = \beta \end{cases}$：
**步骤一：化为一阶方程组**
令 $z = y'$，则：
$$\begin{cases} y' = z \\ z' = f(x, y, z) \end{cases}$$
**步骤二：引入初始参数**
设 $y(a) = \alpha$，$z(a) = s$，其中 $s$ 为待定参数。
**步骤三：求解初值问题**
对给定 $s$，用 RK4 求解初值问题，得到 $y(b; s)$。
**步骤四：定义目标函数**
$$F(s) = y(b; s) - \beta$$
求解 $F(s) = 0$ 即得原问题解。
**步骤五：割线法迭代**
$$s_{k+1} = s_k - F(s_k)\cdot\frac{s_k - s_{k-1}}{F(s_k) - F(s_{k-1})}$$
迭代至 $|F(s_k)| < \text{tol}$。
对于导数边界条件，类似处理即可。
### 应用于问题一
令 $z = y'$，化为一阶方程组：
$$\begin{cases} y' = z \\ z' = x - y \end{cases}$$
初始条件：$y(0) = \pi/2$，设 $z(0) = s$
目标函数：$F(s) = y(\pi/2; s) - \pi/2$
**数值结果：**
- 最优参数 $s^* = z(0) \approx 1.000000$（精确解 $y = x + \frac{\pi}{2}\cos x$ 给出 $y'(0) = 1$）
- $F(s^*) \approx -1.11 \times 10^{-15}$

| $N$ | $h$ | 最大误差 |
|:---:|:---:|:--------:|
| 4 | 0.39270（$\pi/8$） | 4.85e-03 |
| 8 | 0.19635（$\pi/16$） | 2.98e-04 |
| 10 | 0.15708 | 1.24e-05 |
| 20 | 0.07854 | 7.79e-07 |
| 50 | 0.03142 | 1.88e-08 |
| 100 | 0.01571 | 4.53e-10 |

RK4 的四阶精度使打靶法具有 $O(h^4)$ 收敛性。

**数值解图**：
![[shooting_p1_solution.png|500]]
图 2-5：打靶法求解问题（一）的数值解与精确解对比（$N=8,\ h=\pi/16$）。
### 应用于问题二
化为一阶方程组：
$$\begin{cases} y' = z \\ z' = 3x + 1 - 2z + 3y \end{cases}$$
由左边界 $y'(1)-y(1)=1$：令 $y(1) = s$，则 $z(1) = s+1$。
目标函数（由右边界定义）：
$$F(s) = z(2; s) + y(2; s) + 4$$
数值结果：
- 最优参数 $s^* = y(1) \approx -2.0000000000$（精确值）
- $F(s^*) \approx 3.29 \times 10^{-14}$

| $N$ |   $h$   |   最大误差   |
| :-: | :-----: | :------: |
|  5  | 0.20000 | 8.88e-16 |
| 10  | 0.10000 | 1.60e-14 |
| 20  | 0.05000 | 1.51e-14 |
| 50  | 0.02000 | 5.77e-15 |
| 100 | 0.01000 | 2.71e-14 |

线性精确解由 RK4 精确重现。

**数值解图**：
![[shooting_p2_solution.png|500]]
图 2-6：打靶法求解问题（二）的数值解与精确解对比（$h=0.1$）
## 2-5 配点法（Collocation Method）
### 方法原理
配点法选择一组基函数近似解，在若干配点处要求微分方程精确成立，从而确定基函数系数。
**步骤一：构造近似解**
选择基函数 $\{\psi_j(x)\}_{j=1}^{n}$，设：
$$y_h(x) = \psi_0(x) + \sum_{j=1}^{n} a_j \psi_j(x)$$
其中 $\psi_0(x)$ 满足非齐次边界条件，$\psi_j(x)$ 满足齐次边界条件。
**步骤二：选取配点**
在区间内选 $n$ 个配点 $x_1, \dots, x_n$（通常等距或 Gauss 点）。
**步骤三：建立配点方程**
在每个配点 $x_k$ 处要求微分方程精确成立，得到 $n$ 个方程关于 $n$ 个系数 $a_j$ 的线性方程组：
$$A\mathbf{a} = \mathbf{b}$$
### 应用于问题一
令 $\psi_0(x) = \pi/2$ 满足非齐次边界条件。基函数选取：
$$\psi_j(x) = x^j\!\left(x - \frac{\pi}{2}\right), \quad j = 1, 2, \dots, n$$
这些基函数满足齐次边界条件 $\psi_j(0) = \psi_j(\pi/2) = 0$。
近似解为：
$$y_h(x) = \frac{\pi}{2} + \sum_{j=1}^{n} a_j\, x^j\!\left(x - \frac{\pi}{2}\right)$$
配点取等距点 $x_k = \dfrac{k\pi}{2(n+1)}$，$k = 1, 2, \dots, n$。
每个配点处要求 $y_h''(x_k) + y_h(x_k) = x_k$，即：
$$\sum_{j=1}^{n} a_j\bigl[\psi_j''(x_k) + \psi_j(x_k)\bigr] = x_k - \frac{\pi}{2}$$
**数值结果**（谱精度）：
配点法在基函数选择适当时具有指数收敛性（谱精度）。

| $n$ | 最大误差 |
|:---:|:--------:|
| 3 | 8.42e-04 |
| 5 | 6.65e-06 |
| 7 | 3.12e-08 |
| 10 | 6.46e-12 |

随着基函数个数 $n$ 增加，误差呈指数衰减——这是谱方法的典型特征（而有限差分/有限元为代数收敛）。

> [!note] 注释 
> 配点法为谱方法，其精度由基函数个数 $n$ 而非网格步长 $h$ 决定，故此处不列出 $h$ 值。取 $n=5$ 即可获得远高于 $h=\pi/16$ 差分法的精度。

**数值解图**：
![[colloc_p1_solution.png|500]]
图 2-7：配点法求解问题（一）的数值解与精确解对比（$n=7$）。配点法以指数收敛速度逼近精确解。
### 应用于问题二
构建 $\psi_0(x)$ 满足非齐次边界条件：设 $\psi_0(x) = ax + b$，代入：
$$\psi_0'(1) - \psi_0(1) = a - (a+b) = -b = 1 \Rightarrow b = -1$$
$$\psi_0'(2) + \psi_0(2) = a + (2a+b) = 3a + b = -4 \Rightarrow 3a - 1 = -4 \Rightarrow a = -1$$
故 $\psi_0(x) = -x - 1$（恰好也是精确解！）。
基函数 $\psi_j(x)$ 需满足齐次边界条件 $\psi_j'(1)-\psi_j(1)=0$，$\psi_j'(2)+\psi_j(2)=0$：
$$\psi_j(x) = (x-1)^j(x-2)^j, \quad j = 1, 2, \dots, n$$
近似解：
$$y_h(x) = -x - 1 + \sum_{j=1}^{n} a_j\,(x-1)^j(x-2)^j$$
在配点 $x_k = 1 + k/(n+1)$ 处要求 $y_h'' + 2y_h' - 3y_h = 3x_k + 1$。
**数值结果**：
由于 $\psi_0(x) = -x-1$ 已是精确解，所有系数 $a_j = 0$，数值解与精确解完全一致（误差 $0$）。
**数值解图**：
![[colloc_p2_solution.png|500]]
图 2-8：配点法求解问题（二）的数值解与精确解对比。$\psi_0(x)=-x-1$ 恰为精确解，数值解与精确解完全重合。

# 3 总结
## 初值问题方法对比

|  方法  |   精度阶    | 每步函数求值次数 | 稳定性  |    优缺点     |
| :--: | :------: | :------: | :--: | :--------: |
| 后退欧拉 |  $O(h)$  |  1（＋迭代）  | A-稳定 | 适合刚性方程，精度低 |
| 改进欧拉 | $O(h^2)$ |    2     | 条件稳定 |  精度/代价平衡好  |
| RK4  | $O(h^4)$ |    4     | 条件稳定 |  精度高，最常用   |

## 边值问题方法对比

| 方法 | 收敛阶 | 特点 |
|:----:|:------:|:----:|
| 差分法 | $O(h^2)$ | 实现简单，适合规则区域 |
| 有限元法 | $O(h^2)$ | 适合复杂边界条件，理论完备 |
| 打靶法 | $O(h^4)$（RK4内层） | 转化为初值问题，需迭代求解 |
| 配点法 | 谱精度（快于任何代数阶） | 基函数选择至关重要 |

# 4 附录：完整 Python 代码
以下代码包含本报告中所有方法的完整实现。
## IVP 方法代码

```python
import math

# =====================
# 问题定义
# =====================

def fun1(t, y):
    """问题1：y' = e^{-t} - y"""
    return math.exp(-t) - y

def fun2(t, y):
    """问题2：y' = y^2(cos t - sin t) - y"""
    return y * y * (math.cos(t) - math.sin(t)) - y

def exact1(t):
    """问题1精确解：y = (t+1)e^{-t}"""
    return (t + 1) * math.exp(-t)

def exact2(t):
    """问题2精确解：y = 1/(e^t - sin t)"""
    return 1.0 / (math.exp(t) - math.sin(t))

# =====================
# 后退欧拉法
# =====================

def backward_euler(f, t0, y0, target_t, h, max_iter=100, tol=1e-12):
    """
    后退欧拉法（隐式欧拉法）
    对非线性问题采用不动点迭代
    """
    n = int((target_t - t0) / h)
    t, y = t0, y0
    for _ in range(n):
        t_next = t + h
        # 不动点迭代求解隐式方程
        y_next = y
        for _ in range(max_iter):
            y_next_new = y + h * f(t_next, y_next)
            if abs(y_next_new - y_next) < tol:
                y_next = y_next_new
                break
            y_next = y_next_new
        y, t = y_next, t_next
    return y


# =====================
# 改进欧拉法（梯形法）
# =====================

def improved_euler(f, t0, y0, target_t, h):
    """
    改进欧拉法（预测-校正格式）
    预测：ỹ_{n+1} = y_n + h·f(t_n, y_n)
    校正：y_{n+1} = y_n + h/2·[f(t_n, y_n) + f(t_{n+1}, ỹ_{n+1})]
    """
    n = int((target_t - t0) / h)
    t, y = t0, y0
    for _ in range(n):
        k1 = f(t, y)
        k2 = f(t + h, y + h * k1)
        y = y + (h / 2) * (k1 + k2)
        t = t + h
    return y


# =====================
# 经典 RK4
# =====================

def rk4(f, t0, y0, target_t, h):
    """
    经典四阶龙格-库塔法
    k1 = f(t_n, y_n)
    k2 = f(t_n + h/2, y_n + h·k1/2)
    k3 = f(t_n + h/2, y_n + h·k2/2)
    k4 = f(t_n + h, y_n + h·k3)
    y_{n+1} = y_n + h/6·(k1 + 2k2 + 2k3 + k4)
    """
    n = int((target_t - t0) / h)
    t, y = t0, y0
    for _ in range(n):
        k1 = f(t, y)
        k2 = f(t + h / 2, y + (h / 2) * k1)
        k3 = f(t + h / 2, y + (h / 2) * k2)
        k4 = f(t + h, y + h * k3)
        y = y + (h / 6) * (k1 + 2 * k2 + 2 * k3 + k4)
        t = t + h
    return y


# =====================
# 测试 IVP 方法
# =====================

if __name__ == '__main__':
    t0, target = 0.0, 1.0

    print('=' * 60)
    print('IVP 数值结果')
    print('=' * 60)

    for prob_name, fun, y0, exact in [
        ('问题1：y\'=e⁻ᵗ-y', fun1, 1.0, exact1),
        ('问题2：y\'+y=y²(cost-sint)', fun2, 1.0, exact2)
    ]:
        print(f'\n--- {prob_name} ---')
        print(f'精确解 y(1) = {exact(1):.10f}')

        for method_name, method in [
            ('后退欧拉法', backward_euler),
            ('改进欧拉法', improved_euler),
            ('RK4', rk4)
        ]:
            print(f'\n  {method_name}:')
            for h in [0.1, 0.05, 0.01, 0.005, 0.001]:
                y_approx = method(fun, t0, y0, target, h)
                err = abs(y_approx - exact(1))
                print(f'    h={h:.3f}: y≈{y_approx:.10f}, 误差={err:.2e}')
```

## BVP 方法代码
```python
import math
import numpy as np

# =====================
# BVP 问题定义
# =====================

a1, b1 = 0.0, math.pi / 2   # 问题1区间
a2, b2 = 1.0, 2.0            # 问题2区间

def exact_bvp1(x):
    """问题1精确解：y = x + (π/2)cos(x)"""
    return x + (math.pi / 2) * math.cos(x)

def exact_bvp2(x):
    """问题2精确解：y = -x - 1"""
    return -x - 1


# =====================
# 1. 差分法
# =====================

def finite_diff_p1(N):
    """差分法求解 y'' + y = x, y(0)=y(π/2)=π/2"""
    h = (b1 - a1) / N
    x = np.linspace(a1, b1, N + 1)
    A = np.zeros((N - 1, N - 1))
    rhs = np.zeros(N - 1)
    for i in range(N - 1):
        A[i, i] = -2 + h * h
        if i > 0:
            A[i, i - 1] = 1
        if i < N - 2:
            A[i, i + 1] = 1
        rhs[i] = h * h * x[i + 1]
    yL = yR = math.pi / 2
    rhs[0] -= yL
    rhs[-1] -= yR
    y_inner = np.linalg.solve(A, rhs)
    y = np.zeros(N + 1)
    y[0] = yL
    y[-1] = yR
    y[1:-1] = y_inner
    return x, y


def finite_diff_p2(N):
    """差分法求解 y''+2y'-3y=3x+1, y'(1)-y(1)=1, y'(2)+y(2)=-4"""
    h = (b2 - a2) / N
    x = np.linspace(a2, b2, N + 1)
    A = np.zeros((N + 1, N + 1))
    rhs = np.zeros(N + 1)

    # 左边界: y1 - (1+h)y0 = h
    A[0, 0] = -(1 + h)
    A[0, 1] = 1
    rhs[0] = h

    # 内点
    a = 1 / h / h - 1 / h
    d = -2 / h / h - 3
    c = 1 / h / h + 1 / h
    for i in range(1, N):
        A[i, i - 1] = a
        A[i, i] = d
        A[i, i + 1] = c
        rhs[i] = 3 * x[i] + 1

    # 右边界: (1+h)yN - y_{N-1} = -4h
    A[N, N - 1] = -1
    A[N, N] = 1 + h
    rhs[N] = -4 * h

    y = np.linalg.solve(A, rhs)
    return x, y


# =====================
# 2. 有限元法
# =====================

def fem_p1(N):
    """
    有限元法求解 y'' + y = x, y(0)=y(π/2)=π/2
    弱形式：-∫y'v' + ∫yv = ∫xv
    """
    h = (b1 - a1) / N
    x = np.linspace(a1, b1, N + 1)
    A = np.zeros((N - 1, N - 1))
    f = np.zeros(N - 1)
    yL = yR = math.pi / 2

    diag = -1 / h + h / 3
    offd = 1 / h + h / 6

    for e in range(1, N + 1):
        xL, xR = x[e - 1], x[e]
        le = np.array([h * (2 * xL + xR) / 6, h * (xL + 2 * xR) / 6])

        if e > 1:  # local DOF 0 => global node e-1 (interior)
            f[e - 2] += le[0]
        if e < N:  # local DOF 1 => global node e (interior)
            f[e - 1] += le[1]

        if 1 < e < N:
            i, j = e - 2, e - 1
            A[i, i] += diag
            A[i, j] += offd
            A[j, i] += offd
            A[j, j] += diag
        elif e == 1:
            i = 0
            A[i, i] += diag
            f[i] -= offd * yL
        elif e == N:
            i = N - 2
            A[i, i] += diag
            f[i] -= offd * yR

    y_in = np.linalg.solve(A, f)
    y = np.zeros(N + 1)
    y[0] = yL
    y[-1] = yR
    y[1:-1] = y_in
    return x, y


def fem_p2(N):
    """
    有限元法求解 y''+2y'-3y=3x+1
    弱形式含 Robin 边界项
    """
    h = (b2 - a2) / N
    x = np.linspace(a2, b2, N + 1)
    A = np.zeros((N + 1, N + 1))
    f = np.zeros(N + 1)

    for e in range(1, N + 1):
        xL, xR = x[e - 1], x[e]
        # 单元刚度：-∫y'v' + 2∫y'v - 3∫yv
        Ks = np.array([[1 / h, -1 / h], [-1 / h, 1 / h]])
        Kc = 0.5 * np.array([[-1, 1], [-1, 1]])
        Km = (h / 6) * np.array([[2, 1], [1, 2]])
        Ke = -Ks + 2 * Kc - 3 * Km

        le = np.array([h * (2 * (3 * xL + 1) + (3 * xR + 1)) / 6,
                       h * ((3 * xL + 1) + 2 * (3 * xR + 1)) / 6])

        i, j = e - 1, e
        A[i, i] += Ke[0, 0]
        A[i, j] += Ke[0, 1]
        A[j, i] += Ke[1, 0]
        A[j, j] += Ke[1, 1]
        f[i] += le[0]
        f[j] += le[1]

    # Robin 边界贡献
    A[0, 0] += -1   # -y(1)v(1)
    A[N, N] += -1   # -y(2)v(2)
    f[0] += 1       # +1·v(1)
    f[N] += 4       # +4·v(2)

    y = np.linalg.solve(A, f)
    return x, y


# =====================
# 3. 打靶法
# =====================

def shooting_p1(N, guess1=-2.0, guess2=0.0, tol=1e-12, max_iter=50):
    """打靶法求解 y'' + y = x, RK4 内层 + 割线法"""
    h = (b1 - a1) / N

    def solve(s):
        t, y, z = a1, math.pi / 2, s
        for _ in range(N):
            k1y, k1z = z, t - y
            k2y, k2z = z + h / 2 * k1z, (t + h / 2) - (y + h / 2 * k1y)
            k3y, k3z = z + h / 2 * k2z, (t + h / 2) - (y + h / 2 * k2y)
            k4y, k4z = z + h * k3z, (t + h) - (y + h * k3y)
            y += h / 6 * (k1y + 2 * k2y + 2 * k3y + k4y)
            z += h / 6 * (k1z + 2 * k2z + 2 * k3z + k4z)
            t += h
        return y

    s_prev, s_curr = guess1, guess2
    F_prev = solve(s_prev) - math.pi / 2
    F_curr = solve(s_curr) - math.pi / 2

    for _ in range(max_iter):
        if abs(F_curr) < tol:
            break
        if abs(F_curr - F_prev) < 1e-15:
            break
        s_next = s_curr - F_curr * (s_curr - s_prev) / (F_curr - F_prev)
        s_prev, s_curr = s_curr, s_next
        F_prev, F_curr = F_curr, solve(s_curr) - math.pi / 2

    return s_curr, F_curr


def shooting_p2(N, guess1=0.0, guess2=2.0, tol=1e-12, max_iter=50):
    """打靶法求解 y''+2y'-3y=3x+1，含导数边界条件"""
    h = (b2 - a2) / N

    def solve(s):
        t, y, z = a2, s, s + 1  # 左边界：z(1)=y(1)+1
        for _ in range(N):
            k1y, k1z = z, 3 * t + 1 - 2 * z + 3 * y
            k2y, k2z = z + h / 2 * k1z, 3 * (t + h / 2) + 1 - 2 * (z + h / 2 * k1z) + 3 * (y + h / 2 * k1y)
            k3y, k3z = z + h / 2 * k2z, 3 * (t + h / 2) + 1 - 2 * (z + h / 2 * k2z) + 3 * (y + h / 2 * k2y)
            k4y, k4z = z + h * k3z, 3 * (t + h) + 1 - 2 * (z + h * k3z) + 3 * (y + h * k3y)
            y += h / 6 * (k1y + 2 * k2y + 2 * k3y + k4y)
            z += h / 6 * (k1z + 2 * k2z + 2 * k3z + k4z)
            t += h
        return z + y + 4  # F(s) = y'(2) + y(2) + 4

    s_prev, s_curr = guess1, guess2
    F_prev, F_curr = solve(s_prev), solve(s_curr)

    for _ in range(max_iter):
        if abs(F_curr) < tol:
            break
        if abs(F_curr - F_prev) < 1e-15:
            break
        s_next = s_curr - F_curr * (s_curr - s_prev) / (F_curr - F_prev)
        s_prev, s_curr = s_curr, s_next
        F_prev, F_curr = F_curr, solve(s_curr)

    return s_curr, F_curr


# =====================
# 4. 配点法
# =====================

def collocation_p1(n):
    """
    配点法求解 y'' + y = x
    基函数: ψ_j(x) = x^j·(x - π/2)
    """
    xs = np.array([k * math.pi / (2 * (n + 1)) for k in range(1, n + 1)])
    A = np.zeros((n, n))
    b_vec = np.zeros(n)

    for k in range(n):
        xk = xs[k]
        b_vec[k] = xk - math.pi / 2
        for j in range(1, n + 1):
            psi = xk ** j * (xk - math.pi / 2)
            # psi'' = j(j-1)·x^(j-2)·(x-π/2) + 2j·x^(j-1)
            if j == 1:
                psi_pp = 2.0
            else:
                psi_pp = (j * (j - 1) * xk ** (j - 2) * (xk - math.pi / 2)
                          + 2 * j * xk ** (j - 1))
            A[k, j - 1] = psi_pp + psi  # y'' + y

    a_coeff = np.linalg.solve(A, b_vec)
    return a_coeff


def eval_colloc_p1(x, a_coeff):
    """计算配点法在 x 处的近似解"""
    n = len(a_coeff)
    y = math.pi / 2
    for j in range(1, n + 1):
        y += a_coeff[j - 1] * x ** j * (x - math.pi / 2)
    return y


def collocation_p2(n):
    """
    配点法求解 y''+2y'-3y=3x+1
    ψ₀(x) = -x-1（满足非齐次边界条件）
    ψ_j(x) = (x-1)^j·(x-2)^j（满足齐次边界条件）
    """
    xs = np.array([1 + k / (n + 1) for k in range(1, n + 1)])
    A = np.zeros((n, n))
    b_vec = np.zeros(n)

    for k in range(n):
        xk = xs[k]
        # L[ψ₀] = 3x_k + 1，因为ψ₀恰恰是精确解
        b_vec[k] = 0.0  # 3x_k+1 - (3x_k+1) = 0

        u, v = xk - 1, xk - 2
        for j in range(1, n + 1):
            if j == 1:
                psi = u * v
                psi_p = u + v
                psi_pp = 2.0
            else:
                psi = u ** j * v ** j
                psi_p = j * u ** (j - 1) * v ** (j - 1) * (u + v)
                psi_pp = (j * (j - 1) * u ** (j - 2) * v ** (j - 2) * (u + v) ** 2
                          + 2 * j * u ** (j - 1) * v ** (j - 1))
            L_psi = psi_pp + 2 * psi_p - 3 * psi
            A[k, j - 1] = L_psi

    a_coeff = np.linalg.solve(A, b_vec)
    return a_coeff


# =====================
# 测试 BVP 方法
# =====================

if __name__ == '__main__':
    np.set_printoptions(precision=4, suppress=True)

    print('=' * 60)
    print('BVP 数值结果')
    print('=' * 60)

    # --- 差分法 ---
    print('\n--- 差分法 ---')
    for N in [4, 8, 16, 32]:
        x, y = finite_diff_p1(N)
        err1 = max(abs(y[i] - exact_bvp1(x[i])) for i in range(N + 1))
        x, y = finite_diff_p2(N)
        err2 = max(abs(y[i] - exact_bvp2(x[i])) for i in range(N + 1))
        print(f'  N={N:2d}: 问题1 误差={err1:.2e}, 问题2 误差={err2:.2e}')

    # --- 有限元法 ---
    print('\n--- 有限元法 ---')
    for N in [4, 8, 16, 32]:
        x, y = fem_p1(N)
        err1 = max(abs(y[i] - exact_bvp1(x[i])) for i in range(N + 1))
        x, y = fem_p2(N)
        err2 = max(abs(y[i] - exact_bvp2(x[i])) for i in range(N + 1))
        print(f'  N={N:2d}: 问题1 误差={err1:.2e}, 问题2 误差={err2:.2e}')

    # --- 打靶法 ---
    print('\n--- 打靶法 ---')
    s1, F1 = shooting_p1(100)
    print(f'  问题1: s*={s1:.6f}, F(s*)={F1:.2e}')
    s2, F2 = shooting_p2(200)
    print(f'  问题2: s*={s2:.6f}, F(s*)={F2:.2e}')

    # --- 配点法 ---
    print('\n--- 配点法 ---')
    for n in [3, 5, 7, 10]:
        a = collocation_p1(n)
        xs_check = np.linspace(0, math.pi / 2, 1000)
        errs = [abs(eval_colloc_p1(x, a) - exact_bvp1(x)) for x in xs_check]
        print(f'  问题1 n={n:2d}: 最大误差={max(errs):.2e}')
```
