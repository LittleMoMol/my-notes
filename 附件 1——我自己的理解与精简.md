# 大框架
机器狗的动作包括**开始**、**结束**、**移动**、**切换测向机频道**、**检测**、**精确定位并清除**等
其中**移动**与**切换测向机频道**没有独立的指令，由模拟器根据输入参数自动推断。
**光学精确定位合并入清除操作**，根据清除指令的返回结果区分精确定位是否成功。

通信协议中仅有4条指令：/enter、/exit、/measure、/clear
![[Pasted image 20260910210414.png|625]]

# 开始 `/enter` 
机器狗进入初始位置，开始测试，虚拟世界时钟开始计时，指令本身不耗时。
# 检测 `/measure`
机器狗在发送检测指令 `/measure` 时，指定**位置参数**和**频道参数**，自动判断位置是否移动，频道是否更改。
完成一次检测指令消耗的总时间包括：**机器狗移动的耗时**、**切换频道的耗时**、**检测耗时**。
如果机器狗未移动或未切换频道，则相应的耗时为 0。
如果机器狗移动了位置，则移动耗时等于**前后两点间的直线距离除以机器狗的速度** (5m/s)。在任何两个频道之间切换的耗时为 1 秒。

检测结果分为三种情况：
- **无信号**：检测点处接收不到指定频道的干扰源信号（该频道无未清除干扰源 / 超出接收距离 / 在定向覆盖角度范围外）。模拟器返回 `no_signal`。
- **距离过近**：当检测点与指定频道的干扰源的距离不超过 5 米并且位于干扰源信号覆盖角度范围内时，由于信号太强，将无法获得示向度信息。模拟器返回 `near`。
- **有信号**：检测点处可以接收到指定频道的干扰源信号，则模拟器返回 `direction` 以及该干扰源的 `示向度`。*注*：示向度与干扰源最大场强方向的真实方位角存在一定的误差，参见题目正文。
- *注*：无论哪种情况，检测耗时均为5秒。
# 清除 `/clear` 
发送清除指令 `/clear` 时，指定**位置参数**和**频道参数**，精确定位并清除指定位置周围不超过 20 米范围内指定频道的干扰源。
由于机器狗使用光学和激光类仪器进行精确定位和清除，因此清除操作中的频道参数仅用于指定要尝试清除的干扰源，不会切换测向机的当前频道状态，也不会产生切换频道的耗时。
模拟器根据**位置参数**，自动推断与上一指令相比机器狗是否移动了位置。指令发出后，先启用光学探测仪进行精确定位，耗时 3 秒。如果在 20 米范围内没有发现指定频道的干扰源，则返回“**未发现**”；如果发现指定频道的干扰源，则立即启用激光枪清除干扰源，耗时 2 秒。

> [!question]  不完全有把握的结论：
> 
> 在绝大多数情况下，这个“位置参数”就应该写机器狗当前所在的位置。对吗？

同一个干扰源只能被清除一次。如果第二次尝试对同一干扰源进行清除，则返回“未发现”。
*一次清除指令消耗的总时间包括*：**机器狗移动的耗时**、**精确定位耗时**、**清除耗时**。移动的耗时与检测指令中移动耗时的设置相同。

返回结果分为两种情况：
**没有发现**：20米范围内不存在指定频道的干扰源，返回 `no_target_in_range`，耗时 3 秒。
**成功清除**：20米范围内发现指定频道的干扰源并成功清除，返回 `success`，耗时 5 秒。

# 结束 `/exit` 
机器狗结束测试。虚拟世界时钟结束计时，指令本身不耗时。机器狗可以在任意位置结束此次测试。

# 举个栗子
![[Pasted image 20260910213421.png|600]]
# 机器狗接口
默认接口地址为 `http://127.0.0.1:2026`。可以在模拟器空闲时修改端口。模拟器启动时会检测并保留端口；端口被其他应用程序占用时，应先关闭占用程序，或在设置中选择其他可用端口。
5 秒倒计时期间、非测试期间和测试结束后，机器狗接口均未开放，连接失败属于正常现象。机器狗程序应使用当前登录参赛队号作为 `robot_id`，每个新动作使用新的 `request_id`，并逐次等待响应，不得并发发送不同动作。网络中断后重试同一个动作时，必须复用原请求内容和原 `request_id`。
# 测试过程和结束
测试页面的“指令与反馈”区域显示机器狗请求及模拟器反馈，默认仅显示最新1000条，可以在设置中调整为100至5000条。这只是界面显示数量，不限制机器狗请求总数，完整行为仍会写入日志。

# 测试案例编码和行为日志
每局测试都会显示一个**测试案例编码**，**并在对应测试模块的日志列表中长期显示**。测试案例编码用于表格填写和日志关联。
演练测试结束后，界面可以显示**本局干扰源总数**、**全向干扰源数量**和**定向干扰源数量**。正式测试在任何界面和完成提示中都不显示这些案例真值。
正式测试结束后，模拟器自动生成加密机器狗行为日志并加入上传队列。上传失败时模拟器自动重试。
选手可以从日志列表导出正式日志，放入支撑材料中。
加密日志文件大小需在2MB以内方可上传，正式测试前确认机器狗程序**没有异常高频循环**。
机器狗程序应自行记录测试过程中的指令序列、响应信息等内容。